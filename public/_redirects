# TinaCMS admin - serve the actual HTML file
/admin/index.html /admin/index.html 200

# Custom admin dashboard
/admin /admin 200

# SPA fallback for any routes (but exclude admin paths)
/* /index.html 200
